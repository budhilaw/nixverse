# Template for: secrets/budhilaw-gpg.yaml
#
# DO NOT commit this file — it's just an example.
# Create the real file with: nix run nixpkgs#sops -- secrets/budhilaw-gpg.yaml
#
# Export your GPG private key:
#   gpg --export-secret-keys --armor 0xBD838B746BAA8C5F > /tmp/gpg-key.asc
#
# Then paste the content into the sops editor when it opens.

gpg_private_key: |
    -----BEGIN PGP PRIVATE KEY BLOCK-----

    PASTE_EXPORTED_GPG_PRIVATE_KEY_HERE

    -----END PGP PRIVATE KEY BLOCK-----
